#pragma once

namespace ZC_GUI_Bindings
{
    const unsigned int bind_Border = 0;
    const unsigned int bind_BL = 1;
    const unsigned int bind_ObjData = 2;
    
    const unsigned int bind_tex_Icons = 0;
    const unsigned int bind_tex_Text = 1;
    const unsigned int bind_ColorManipulator_resultTriangle = 10;   //  not real binding uses to draw quad withought texture, only with seted rgba color
    const unsigned int bind_ColorManipulator_saturationTrianlge = 11;   //  not real binding uses to draw quad withought texture, draw quad with interpolated colors
    const unsigned int bind_ColorManipulator_alphaTrinalge = 12;   //  not real binding uses to draw quad withought texture, draw quad with interpolated colors
}